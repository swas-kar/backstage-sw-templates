app:
  name: {{app_name}}

${{ parameters.component_id }}

'{{ parameters.component_id }}'

{{ parameters.component_id }}

${{ values.app_name }}
'{{ values.app_name }}'
'${{ values.app_name }}'
